// dial.js — placeholder.
// Renderiza o disco SVG (4 anéis, core breathing, ghost trail, anel de antecipação,
// guia vertical 12h, marcador especial do "último beep").
// API: { render(rootEl), markTap(diffMs, tier, isLastBeep), pulseBeat(beatIndex, totalBeats), reset() }
export function render(){ /* TODO */ }
